{
  "name": "matsumoto-now",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "fetch-data": "node scripts/fetch-city-data.mjs",
    "fetch-monthly": "node scripts/fetch-crime-data.mjs && node scripts/fetch-bus-data.mjs && node scripts/fetch-shelter-data.mjs && node scripts/fetch-garbage-data.mjs",
    "fetch-air": "node scripts/fetch-air-data.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^5.12.0",
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.12",
    "fflate": "^0.8.3",
    "playwright": "^1.62.0"
  }
}
